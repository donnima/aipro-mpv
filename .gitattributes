# Normalize all text files to LF in the repository.
# Prevents CRLF churn between Windows development and Linux CI.
* text=auto eol=lf

# Binary files — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
